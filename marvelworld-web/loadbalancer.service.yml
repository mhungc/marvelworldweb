apiVersion: v1
kind: Service
metadata:
 name: nginx-loadbalancer
spec:
 type: LoadBalancer
 selector:
    app: my-backend
 ports:
  - name: "9000"
    port: 9000
    targetPort: 80
    
  - name: "443"
    port: 443
    targetPort: 443